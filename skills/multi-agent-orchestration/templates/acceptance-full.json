{
  "level": "verified",
  "criteria": [
    {
      "id": "build",
      "must": "Full build succeeds with 0 errors",
      "severity": "required",
      "evidence": [
        "commands-run",
        "validation-output"
      ]
    },
    {
      "id": "tests",
      "must": "The required test suites pass",
      "severity": "required",
      "evidence": [
        "commands-run",
        "validation-output"
      ]
    }
  ],
  "evidence": [
    "changed-files",
    "commands-run",
    "validation-output",
    "residual-risks"
  ],
  "verify": [
    {
      "id": "build",
      "command": "npm run build",
      "timeoutMs": 300000
    },
    {
      "id": "tests",
      "command": "npm test",
      "timeoutMs": 600000
    }
  ]
}
