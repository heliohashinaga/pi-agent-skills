#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/pi-subagents@0.43.0_@earendil-works+pi-agent-core@0.83.0_ws@8.21.2_zod@4.4.3__@earendil_f35508c8b34a1d059c26f2bce4a3daef/node_modules/pi-subagents/node_modules:/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/pi-subagents@0.43.0_@earendil-works+pi-agent-core@0.83.0_ws@8.21.2_zod@4.4.3__@earendil_f35508c8b34a1d059c26f2bce4a3daef/node_modules:/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/pi-subagents@0.43.0_@earendil-works+pi-agent-core@0.83.0_ws@8.21.2_zod@4.4.3__@earendil_f35508c8b34a1d059c26f2bce4a3daef/node_modules/pi-subagents/node_modules:/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/pi-subagents@0.43.0_@earendil-works+pi-agent-core@0.83.0_ws@8.21.2_zod@4.4.3__@earendil_f35508c8b34a1d059c26f2bce4a3daef/node_modules:/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../pi-subagents/install.mjs" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../pi-subagents/install.mjs" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../pi-subagents/install.mjs" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../pi-subagents/install.mjs" "$@"
else
  exec node  "$basedir/../pi-subagents/install.mjs" "$@"
fi
# cmd-shim-target=/home/helio/repos/pi-agent-skills/extensions/devloop/node_modules/pi-subagents/install.mjs
